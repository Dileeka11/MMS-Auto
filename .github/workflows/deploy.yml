name: Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy-frontend:
    name: Build & Deploy Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        env:
          VITE_API_URL: /api
        run: npm run build

      - name: Deploy frontend via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./frontend/dist/
          server-dir: /
          exclude: |
            **/.git*
            **/.git*/**
            api/**
            laravel/**
            .well-known/**
            cgi-bin/**

  deploy-backend:
    name: Build & Deploy Backend (Laravel)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, bcmath, pdo, pdo_mysql, tokenizer, xml, ctype, json, fileinfo, openssl
          tools: composer:v2

      - name: Install composer dependencies
        working-directory: backend
        run: composer install --no-dev --optimize-autoloader --prefer-dist --no-interaction

      - name: Create production .env
        working-directory: backend
        env:
          ENV_CONTENT: ${{ secrets.PROD_ENV_FILE }}
        run: printf '%s' "$ENV_CONTENT" > .env

      - name: Cache config & routes
        working-directory: backend
        run: |
          php artisan config:cache
          php artisan route:cache
          php artisan view:cache

      - name: Prepare Laravel app folder (without public/)
        run: |
          mkdir -p deploy/laravel
          rsync -a --exclude='public' --exclude='node_modules' --exclude='tests' --exclude='.git' --exclude='.env.example' backend/ deploy/laravel/

      - name: Prepare Laravel public folder
        run: |
          mkdir -p deploy/api
          cp -r backend/public/. deploy/api/
          # Fix index.php paths to point one level up to /laravel
          sed -i "s|__DIR__.'/../storage|__DIR__.'/../laravel/storage|g" deploy/api/index.php
          sed -i "s|__DIR__.'/../vendor|__DIR__.'/../laravel/vendor|g" deploy/api/index.php
          sed -i "s|__DIR__.'/../bootstrap|__DIR__.'/../laravel/bootstrap|g" deploy/api/index.php

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Deploy Laravel app (/laravel) via lftp
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USERNAME }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
        run: |
          lftp -c "
          set ftp:ssl-allow no;
          set net:timeout 30;
          set net:max-retries 5;
          set net:reconnect-interval-base 5;
          set net:reconnect-interval-multiplier 1;
          set ftp:passive-mode true;
          set mirror:parallel-transfer-count 5;
          set mirror:use-pget-n 3;
          open -u \"$FTP_USER\",\"$FTP_PASS\" \"$FTP_HOST\";
          mirror -R --parallel=5 --only-newer --no-perms --verbose=1 ./deploy/laravel/ /laravel/;
          "

      - name: Deploy Laravel public (/api) via lftp
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USERNAME }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
        run: |
          lftp -c "
          set ftp:ssl-allow no;
          set net:timeout 30;
          set net:max-retries 5;
          set ftp:passive-mode true;
          open -u \"$FTP_USER\",\"$FTP_PASS\" \"$FTP_HOST\";
          mirror -R --parallel=5 --only-newer --no-perms --verbose=1 ./deploy/api/ /api/;
          "
